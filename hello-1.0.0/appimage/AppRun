#!/bin/bash
# AppRun entrypoint script for hello AppImage

set -e

# Resolve AppDir root
SELF=$(readlink -f "$0")
HERE=${SELF%/*}

# Set up environment
export PATH="${HERE}/usr/bin:${PATH}"
export LD_LIBRARY_PATH="${HERE}/usr/lib:${HERE}/usr/lib64:${LD_LIBRARY_PATH:-}"
export XDG_DATA_DIRS="${HERE}/usr/share:${XDG_DATA_DIRS:-/usr/local/share:/usr/share}"
export MANPATH="${HERE}/usr/share/man:${MANPATH:-}"

# Check invocation name
INVOCATION_NAME="$(basename "$0")"
if [ "$INVOCATION_NAME" = "hello_rust" ] || [ "$INVOCATION_NAME" = "hello-rust" ]; then
    exec "${HERE}/usr/bin/hello_rust" "$@"
elif [ "$INVOCATION_NAME" = "hello_go" ]; then
    exec "${HERE}/usr/bin/hello_go" "$@"
fi

# Default to C implementation
exec "${HERE}/usr/bin/hello" "$@"
